{
  "version": 2,
  "builds": [
    {
      "src": "server/app.js",  // Adjust this to your main backend file
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",  // API routes
      "dest": "server/app.js"
    }
  ]
}
